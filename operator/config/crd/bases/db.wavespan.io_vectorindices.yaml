---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: vectorindices.db.wavespan.io
spec:
  group: db.wavespan.io
  names:
    kind: VectorIndex
    listKind: VectorIndexList
    plural: vectorindices
    singular: vectorindex
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VectorIndex declares a vector index.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VectorIndexSpec configures a vector index (design/12).
            properties:
              approximate:
                description: ApproximateIndexSpec configures the ANN index (inert
                  until M10 at runtime).
                properties:
                  efConstruction:
                    format: int32
                    type: integer
                  efSearchDefault:
                    format: int32
                    type: integer
                  enabled:
                    type: boolean
                  m:
                    format: int32
                    type: integer
                type: object
              clusterRef:
                type: string
              collection:
                type: string
              dimensions:
                format: int32
                minimum: 1
                type: integer
              dtype:
                type: string
              exact:
                description: ExactIndexSpec configures exact search.
                properties:
                  enabled:
                    type: boolean
                type: object
              label:
                type: string
              metric:
                type: string
              property:
                type: string
              visibility:
                type: string
            required:
            - clusterRef
            - dimensions
            type: object
        type: object
    served: true
    storage: true
