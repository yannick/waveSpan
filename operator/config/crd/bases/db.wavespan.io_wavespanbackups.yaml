---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: wavespanbackups.db.wavespan.io
spec:
  group: db.wavespan.io
  names:
    kind: WaveSpanBackup
    listKind: WaveSpanBackupList
    plural: wavespanbackups
    singular: wavespanbackup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WaveSpanBackup requests a cluster backup.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WaveSpanBackupSpec requests a backup (design/12; wired to
              wavesdb object-store in M12).
            properties:
              clusterRef:
                type: string
              destination:
                type: string
            required:
            - clusterRef
            - destination
            type: object
        type: object
    served: true
    storage: true
