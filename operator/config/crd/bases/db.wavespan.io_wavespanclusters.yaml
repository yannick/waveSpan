---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: wavespanclusters.db.wavespan.io
spec:
  group: db.wavespan.io
  names:
    kind: WaveSpanCluster
    listKind: WaveSpanClusterList
    plural: wavespanclusters
    singular: wavespancluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyMembers
      name: Ready
      type: integer
    - jsonPath: .status.desiredMembers
      name: Desired
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WaveSpanCluster deploys and reconciles a WaveSpan data cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WaveSpanClusterSpec is the desired state of a WaveSpan cluster
              (design/12).
            properties:
              clusterId:
                description: ClusterID is the logical cluster id.
                minLength: 1
                type: string
              disruption:
                description: DisruptionSpec configures the PodDisruptionBudget (design/09).
                properties:
                  maxUnavailable:
                    format: int32
                    minimum: 0
                    type: integer
                type: object
              gateway:
                description: GatewaySpec configures the optional stateless gateway
                  (design/09).
                properties:
                  enabled:
                    type: boolean
                  replicas:
                    format: int32
                    type: integer
                type: object
              image:
                description: Image is the wavespan-node container image.
                minLength: 1
                type: string
              replicas:
                description: Replicas is the number of data pods.
                format: int32
                minimum: 1
                type: integer
              replicationPolicyRef:
                description: ReplicationPolicyRef references a ReplicationPolicy in
                  the same namespace.
                type: string
              security:
                description: SecuritySpec configures transport security.
                properties:
                  insecureDevMode:
                    type: boolean
                  tlsSecretName:
                    type: string
                type: object
              storage:
                description: StorageSpec configures the data PVCs (design/09 "Storage").
                properties:
                  storageClassName:
                    description: StorageClassName selects the PVC storage class.
                    type: string
                  volumeSize:
                    description: VolumeSize is the size of each data PVC (e.g. "50Gi").
                    minLength: 1
                    type: string
                required:
                - volumeSize
                type: object
              topology:
                description: TopologySpec names the node labels used for topology
                  spread (design/09).
                properties:
                  regionLabel:
                    type: string
                  zoneLabel:
                    type: string
                type: object
            required:
            - clusterId
            - image
            - replicas
            - storage
            type: object
          status:
            description: WaveSpanClusterStatus is the observed state.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              desiredMembers:
                format: int32
                type: integer
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: WaveSpanClusterPhase is the high-level lifecycle phase.
                type: string
              readyMembers:
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
